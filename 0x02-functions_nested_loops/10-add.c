#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 *
 * @d: integers to be checked
 *
 * @n: integer to be checked
 *
 * Return: Value of addition
 */
int add(int n, int d)
{
	int sum;

	sum = n + d;
	return (sum);
}
