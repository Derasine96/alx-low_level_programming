#include "main.h"
/**
 * leet - a function that encodes a string
 * @y: a string to be checked
 * Return: Character
 */
char *leet(char *y)
{
	int b;
	char r[] = "AaEeOoTtLl";

	while (r[b] != '\0')
	{
		for (b = 0; y[b] != '\0'; b++)
		{
			r[0] = 4;
		}
		b++;
	}
	return (y);
}
